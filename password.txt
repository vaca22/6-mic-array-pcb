
测试结果如下：

| 项目 | 结果 |
|------|------|
| **22 端口** | 开放 |
| **SSH 登录** | 成功（用户 `pi`，密码 `raspberry`） | 密码1234
| **主机名** | raspberrypi |
| **系统** | Linux 6.12.47 aarch64 (Debian/Raspberry Pi OS) |
| **用户** | uid=1000(pi)，在 sudo 等组内 |

树莓派 SSH 已可正常使用，你可以在本机用：

```bash
ssh pi@192.168.167.20
```

输入密码 `raspberry` 即可登录。建议登录后执行 `passwd` 修改密码。